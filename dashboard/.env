# /static prefix means build files will be located inside root static directory
# required by Django static files framework (STATIC_URL)
PUBLIC_URL=/static

# / - index.html Django rendered template
# /**/ - index.html (React router DOM routes) (any route should be sent to index.html)
# /static/... - meta files (public) (icons, configs, manifests)
# /static/static/... - static files (compiled) (css, js, media)
